pipeline {
  agent any
  stages {
   stage ('git repo') {
    steps {
    git clone 'https://github.com/gellaboina/sample-java-programs.git'
      steps {
        build
      
      }
    }
   }
  }
}
